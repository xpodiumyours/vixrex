# Ürün Kartı Çalışması — Durum Raporu (2026-09-16)

> Bu dosya Cline'ın kod kanıtlarıyla hazırladığı güncel durum raporudur.
> Kural: Kod + git kaydı esas. Eski ajans notlarıyla çelişirse BU DOSYA kazanır.

## 1. Doğrulanmış gerçekler (git kanıtıyla)

- **main (canlı adayı):** Son commit `13046248` — "chore: urun karti calismasini uretime al" (2026-09-15).
  Zengin ürün kartı 5 faz (PR #491–#495) main'de; Casper doğruladı: **yeni kart canlıya çıktı** (vixrex.com, durum 200 ✓).
- **`work/product-live-ready-20260914` (büyük çalışma dalı):** main'den **249 commit ileride**,
  main'e merge EDİLMEDİ; referans olarak duracak (devir belgesindeki karar).
- **Codex'in "kayıp" commiti BULUNDU — kaybolmamış:**
  - Commit: `6ad96d1c` — "feat(urun): toplu kayit ve fotograf yayin kurallarini tamamla" (2026-09-15, yazar Furkan)
  - Dal: `work/urun-toplu-yukleme-20260915` (main + bu tek commit)
  - İçerik: 32 dosya, +1227/−150; toplu yükleme zengin alanları + görsel yayın kuralları
  - 2 migration: `20260915020000_product_batch_rich_fields.sql`, `20260915030000_product_image_publication.sql`
  - Testler dahil (products-batch, bulk-product-upload, gorsel-sikistirma, Flutter import/yayın testleri)
  - ~~**RİSK: main'de YOK, origin'de (yedekte) YOK — yalnız bu bilgisayarda duruyor.**~~ → **DÜZELTİLDİ (2026-09-16, Cline): origin'de VAR ✓** — `origin/work/urun-toplu-yukleme-20260915` ve `origin/gecici/urun-main-birlesme` (kanıt: `git branch -r --contains 6ad96d1c`). Bu satır 15.09 13:51'deki push'tan önce yazılmıştı; §5'teki "1. adım TAMAMLANDI (push ✓)" doğrudur.
- **`work/urun-gorsel-yayin-20260915`:** kendi commiti YOK (main ile aynı). Hazırlanmış boş dal.
- **Çalışma kopyaları (worktree):** 15+ çalışma kopyası dağınık (`.codex-worktrees/`, Temp klasöründeki faz0–faz5,
  `vixrex-blog-20260911`, `vixrex-seo` vb.).

## 2. Codex commitinin (6ad96d1c) bilinen durumu

- ChatGPT/Codex ekran görüntüsüne göre: commit alındı, otomatik inceleme (code-review) **reddetti**,
  ardından kullanım limiti doldu → doğrulama KAPILARI (typecheck/vitest/build/flutter test) koşmamış olabilir.
- Canlıya alınmadan önce kapıların koşulması GEREKİR.

## 3. Bayat / çelişkili notlar (arşivlenecekler)

- `docs/URUN-KARTI-DEVIR.md` → "Canlıya ÇIKMADI / Vercel kotası" bölümü **BAYAT** (kart canlıya çıktı). · Cline notu: bu dosya **yalnız `main` dalında**; `work/product-live-ready-20260914` çalışma kopyasında hiç yok (kanıt: `git ls-tree main --name-only docs/`). Ararken bulunamaması normaldir.
- `docs/urun-sistemi-haritasi.md` §2 → "main'de OLMAYAN şeyler: zengin öznitelikler…" **BAYAT** (main'de artık var).
- `docs/ILERLEME-TABLOSU.md` (2026-09-09) → eski tarihli.
- Çözüm: bayat bölümlere "BAYAT — bkz. DURUM-20260916.md" bandı + dosyaları `docs/arsiv/` klasörüne taşımak.

## 4. Devam eden kontroller

- [x] Canlı vitrinde yeni kart izi testi → **CANLI ✓** (/v/kiralik-kuafor'da 12 adet "hızlı incele"
      tetikleyicisi + 18 ürün linki; ProductQuickView main'in kodunda ve main canlıda)
- [x] Codex worktree test kapısı kurulum durumu → node_modules KURULU ✓ (testler koşulabilir);
      kaydedilmemiş kod değişikliği YOK (yalnız .scratch yardımcı scriptleri)
      — Cline notu (2026-09-16): bu tespit YALNIZ Codex worktree'si için doğru. Diğer üç worktree'de
      kaydedilmemiş iş VAR (bkz. §12)

## 6. Codex commitinin (6ad96d1c) kazandırdıkları + riskler (kod okundu)

**Kazandırdıkları:**
1. **Toplu yükleme zengin alanları:** Excel/CSV/XML yüklemede artık marka, barkod, SKU,
   stok adedi, stok durumu, kategori adı taşınıyor (main'de YOK).
2. **Fiyat çözümleme veritabanına indi:** `_parse_product_price_amount` — metin fiyatı
   ("1.250 TL" gibi) DB tarafında sayıya çevirir; toplu yüklemede fiyat tutarlılığı.
3. **Görsel yayın kuralı:** `image_publish_minimum` kolonu (varsayılan 3) +
   `guard_product_image_publication` tetikleyicisi.
4. Toplu yükleme tarafında zararlı/boş veri temizliği (`cleanString`).

**⚠ RİSK — devir belgesindeki 4. kararla ÇELİŞİYOR:**
- Devir belgesi: "en az 3 görsel kuralı veritabanı tetikleyicisine KONMAMALI;
  canlıdaki 68 ürünün tamamı 1–2 görselli."
- Codex'in tetikleyicisi: görsel sayısı 3'ten azsa ürün **reddedilmez ama GİZLENİR**
  (`is_visible := false`); yeni ürün eklenirken minimum zorla 3 yapılıyor.
- **Anlamı:** 1–2 görselli mevcut ürünü düzenleyen esnafın ürünü **sessizce vitrinden
  kalkar** (3. görseli ekleyene kadar). Yeni ürünler 1–2 görselle hiç görünmez.
- Canlıya alınmadan önce bu davranış Casper'a sorulmalı / yumuşatılmmalı.
- Ayrıca 2 migrasyon canlı DB'ye uygulanmadan kod deploy edilirse kod-DB uyumsuzluğu olur.

## 7. "Temel tamam mı?" denetimi (2026-09-16, kod kanıtı) — Casper'ın sorusu

**Karar (Casper):** Görsel kuralı için (a) — "en az 3 görsel" kuralı hiç olmayacak.
> ⚠ **DEĞİŞTİ (2026-09-16):** Son karar §9'daki **(c')** — Codex tasarımı kalıyor (eski ürünler muaf,
> yeni ürün 3 fotoğrafa kadar taslak + panelde açıklama, ürün başına ayarlanabilir kolon).
> Bu bölümdeki (a) kararı kayıt olarak duruyor, artık geçerli değil.
→ 6ad96d1c main'e alınırken `20260915030000_product_image_publication.sql` (gizleme
tetikleyicisi) çıkarılacak; kolon eklemesi de gereksiz hale gelir. **Bu adım da geçersiz** — (c') gereği tetikleyici ve kolon Codex tasarımıyla kalıyor (§9).

**Tekli kayıt zemini — MAIN'DE VAR (kanıt):**
- Ortak şema `shared/product_attribute_schema.json`: 8 şablon — fashion(5 alan),
  electronics(6), beauty(6), food(5), home(7), automotive(6), **generic(0), service(0)**
- Flutter tekli ürün editöründe zengin alan editörü ✓ (`product_rich_fields_editor.dart`)
- Web tekli editörde templateKey/attributes işleniyor ✓ (OwnerProductManager)
- DB: `product_categories.template_key` kolonu + 8 şablon kısıtı ✓ (20260915010000)
- Esnaf kategori yönetiminde şablon seçimi: WEB ✓ (#494) + FLUTTER ✓

**YARIM olan yerler (Casper'ın hissi doğru):**
1. **Şablon azlığı:** 19 kanonik kategorinin yalnız 6 alan ailesi tanımlı; kuaför,
   kafe, gözçük vb. şablonu YOK → generic'e (0 özel alan) düşüyor.
2. **Canlıda hiçbir kategori özel şablonda değil:** 38 kategori = generic.
   Esnaf şablon seçene kadar kartta özel alan GÖRÜNMEZ → "hiçbir şey değişmedi" hissi.
3. **Asistan dışı:** Vixrex Asistan bu zengin alanları bilmiyor (niyet sözlüğünde yok).
4. Toplu yükleme zengin alanları (Codex 6ad96d1c) bu temelin üstüne ekleniyor —
   temel VAR ama ince; önce şablon sayısı + kategori eşlemesi genişletilmeli.

**Değerlendirme:** "Temelsiz bina" değil, "ince temel üzerine erken kat". Sıra:
(1) tekli kayıt şablon setini 19 kategoriye genişlet → (2) kategori-şablon eşlemesini
canlıda doldur → (3) toplu yükleme zengin alanlarını main'e al (görsel kuralı (a)'yla)
→ (4) asistanı bu alanlara bağla (Casper kararı: asistan EN SON, eksikler tamamlandıktan
ve örnek firmalarla uçtan uca kontrol yapıldıktan sonra).

## 9. Codex işi → main geçişi (2026-09-16, sürüyor)

- Tüm kapılar yeşil: typecheck ✓, vitest 1531 ✓, build ✓, flutter analyze ✓, flutter test 748 ✓
- Casper kararı: görsel kuralı için **(c') Codex tasarımı kalıyor** (eski ürünler muaf,
  yeni ürün 3 fotoğrafa kadar taslak + panelde açıklama; ürün başına ayarlanabilir kolon).
- Birleşme hazir: `gecici/urun-main-birlesme` (main + 6ad96d1c, çatışmasız, ort stratejisi)
- DB-kod uyumu kanıtlandı: canlı `batch_create_products(uuid,text,jsonb)` ile yeni imza aynı
  (create or replace = gerçek güncelleme; eski kod string gönderir, jsonb'ye çözülür ✓)
- Yön kuralı: önce DB (2 ek) → sonra push (deploy)
- **ENGEL:** Supabase erişim yetkisi yok — access token süresi dolmuş (401),
  servis anahtarı ortam dosyalarında yok (bilinçli silinmiş), pooler URL'sinde şifre yok.
- Ek 1 uygulama denemesi: `supabase db query --file ... --linked` → 401 Unauthorized
- Araç: `sql-uygula.js` yazıldı (exec_sql yöntemi; anahtar gelirse kullanılabilir)
- **Beklenen:** Casper `supabase login` çalıştıracak (tarayıcı onayı) VEYA SQL'leri
  dashboard SQL Editor'de kendisi koşacak → sonra Ek 1, doğrulama, Ek 2, doğrulama, push.

## 8. 19 kategori ↔ ürün şablonu eşlemesi (2026-09-16, öneri — onaya sunuldu)

Kaynak: `shared/business_categories.json` (19 kanonik kategori) + şablon şeması.

| Kategori | Grup | Önerilen şablon | Durum |
|---|---|---|---|
| giyim, butik | perakende | fashion | VAR ✓ |
| gida, firin | gida | food | VAR ✓ |
| kozmetik | perakende | beauty | VAR ✓ |
| dekorasyon | perakende | home | VAR ✓ |
| elektronik | perakende | electronics | VAR ✓ |
| oto_arac | hizmet | automotive | VAR ✓ |
| kuafor, teknik_servis, hizmet_danismanlik, egitim_ders, ev_temizlik, spor_fitness, saglik_yasam | hizmet | service | VAR ama 0 öznitelik (temel hizmet bloğu var: tür/süre/yer/fiyat biçimi) |
| kirtasiye | perakende | generic | ALAN YOK |
| kafe_lokanta | gida | menu (YENİ) | EKSIK |
| pet_shop_veteriner | perakende | pet (YENİ) | EKSIK |
| diger | diger | generic | VAR |

**Yeni şablon önerileri (Trendyol/Hepsiburada mantığında, esnafın anlayacağı Türkçe):**
- **menu** (kafe/lokanta/fırın): Porsiyon boyutu, Gramaj, Sıcak/Soğuk, Vejetaryen,
  Alerjen uyarısı, Son tüketim (fırın)
- **pet** (pet shop): Hayvan türü, Cins/ırk uyarısı, Yaş grubu, Ağırlık/gramaj, İçerik
- **service zenginleştirme** (kuaför vb. 7 hizmet kategorisi): mevcut hizmet bloğu
  (tür, süre, yer, fiyat biçimi) + opsiyonel öznitelikler: Randevu gerekli mi,
  Deneyim/uzmanlık, Garanti (teknik servis)

## 5. Toparlanma planı (2026-09-16, onaya sunuldu)

1. **ACİL — Yedek:** `work/urun-toplu-yukleme-20260915` (6ad96d1c) origin'e pushlanmalı.
   Şu an TEK KOPYA bu bilgisayarda; disk giderse Codex'in 32 dosyalık işi kaybolur.
2. **Doğrulama:** Codex commitinde kapılar koşulacak (typecheck + vitest + build; Flutter analyze/test).
   (Ekran görüntüsüne göre otomatik inceleme reddedilmişti; canlıya alınmadan önce gereklidir.)
3. **Arşivleme:** Bayat notlara "BAYAT — bkz. DURUM-20260916.md" bandı + `docs/arsiv/` taşıma.
4. **Çöp toplama:** Temp içindeki faz0–faz5 scratchpad çalışma kopyaları, boş dal
   `work/urun-gorsel-yayin-20260915` vb. liste çıkarılıp onayla temizlenecek.
   Durum: 1. adım TAMAMLANDI (push ✓, 2026-09-16).

---
## 10. Anahtar kaybı — objektif kök neden analizi (2026-09-16)

**Soru:** Canlı veritabanı anahtarı (service role) nereye kayboldu?

**Kanıt zinciri (dosya taramasıyla):**
1. Anahtarın evi: `public_web/.env.production.local` — `sql-uygula.js` (satır 10-12) ve
   `public_web/_run_index.js` (satır 8-9) tam olarak bu dosyadan okuyordu.
2. O dosya **26.07.2026 21:18**'de Vercel CLI tarafından ÜZERİNE YAZILMIŞ:
   bugün içinde yalnız `NX_DAEMON`, `TURBO_*`, `VERCEL_*` değişkenleri var;
   `SUPABASE_URL` 2 karakter (boş). `.env.production` da 01.09.2026'da aynı şekilde yazılmış.
3. 30 ayar dosyasının tamamı tarandı (worktree'ler, `vixrex-admin`, `vixrex-review` dahil):
   canlı servis anahtarı HİÇBİRİNDE yok. Bulunan tek "service_role" jetonu
   `.env.local.yerel-yedek` içinde ve `iss=supabase-demo` → **yerel test anahtarı, canlıda işe yaramaz.**
   (İlk tarama `SERVICE_ROLE_KEY=ey` kalıbını arıyordu; ikinci tarama yeni tip `sb_secret_` /
   `sb_publishable_` biçimlerini de taradı → canlı gizli anahtar bulunamadı.)
4. Yani anahtar "silinmiş" değil: **Vercel'in kendi komutları dosyanın üzerine yazmış.**
   Canlı anahtar şu an yalnız iki bulutta: Vercel (site onu kullanıyor) + Supabase paneli.

**Erişim denemeleri ve sonuçları:**
- `supabase projects list` → `Unauthorized` (CLI 2.117.0). Kimlik kasasında kayıt var
  (`Supabase CLI:supabase`) ama jeton reddediliyor → giriş yapılan hesap projeye yetkili değil.
  Proje kaydı doğru: `Vixrex Project` / ref `chfulefxczbgurtgavtp` / org `cmtwwdvabgusyaylbjdt`.
- `exec_sql` RPC, açık (publishable) anahtarla → **404** (anon'a açık değil). Anahtarsız SQL kapısı YOK.
- Vercel CLI → `Logged out`; `%APPDATA%\com.vercel.cli\Data\config.json` → 263 bayt (jeton yok).
- `supabase/.temp/pooler-url` → şifre alanı BOŞ; `psql` kurulu değil → doğrudan DB bağlantısı kapalı.
- Git takip listesinde canlı anahtar yok (yalnız `.env.example` iskeleti) → git geçmişinde sızıntı yok ✓

**Kurtarma yolları (Casper'ın emeğine göre sıralı):**
- **A (en kolay, 1 kopyala-yapıştır):** Supabase paneli → Project Settings → **API Keys** →
  `service_role` (veya yeni tip `sb_secret_...`) anahtarını kopyala. Panelde gösterilebilir;
  Vercel'deki değer çoğu zaman bir daha okunamaz.
- **B:** Supabase paneli → Account → **Access Tokens** (`sbp_...`) üret → CLI ile SQL koşma.
  Bu yol, giriş yapılan hesabın projeye yetkili olmasını gerektirir (şu anki giriş yetkisiz).
- **C (anahtarsız, ama elle):** Supabase paneli → **SQL Editor** → 2 migration dosyasını sırayla
  yapıştır-çalıştır.

**Hazır araç:** `sql-uygula.js` bekliyor (exec_sql yerine service anahtarıyla REST erişimine
uyarlanabilir). Anahtar bir kez elde edilince Ek 1 + Ek 2 + doğrulama otomatik yapılabilir.
_Bu dosya canlı durum kaydıdır; her doğrulama buraya işlenir._
## 11. Anahtar sorunu — resmî belge doğrulamaları + kesin karar (2026-09-16)

**Resmî kaynaklardan doğrulanan gerçekler (tahmin değil):**
1. **Management API ile SQL koşma kapısı VAR:** `POST https://api.supabase.com/v1/projects/{ref}/database/query`
   — gövde `{"query":"..."}`; yetki: kişisel erişim jetonu (`sbp_...`) + `database:write` kapsamı.
   Yani jeton elde edilirse 2 ekleme + doğrulama + kategori eşlemesi tamamen otomatik yapılabilir.
2. **Kişisel jeton üretme sayfası:** `https://supabase.com/dashboard/account/tokens`
3. **Anahtar okuma:** Yetki tablosunda "API Configuration → API Keys → Read service key" satırı var →
   panel/API üzerinden servis anahtarı okunabilir (`GET /v1/projects/{ref}/api-keys`).
   Supabase belgesi ayrıca: gizli anahtar paylaşımı için "karşı taraf anahtarı Dashboard → Settings > API Keys'ten okusun" diyor.
4. **Vercel tarafı:** Ayarlar "Config" (sonradan okunabilir) ve "Secret" (kaydedildikten sonra salt-yazma)
   olarak ikiye ayrılmış. Secret ise `vercel env pull` değeri getirmez → Vercel yolu garanti değil.
5. **Canlı uygulamada hazır SQL kapısı YOK:** `public_web/src/app/api` altındaki tüm yollar tarandı
   (account, products, owner-*, paytr, cron, …); hiçbiri SQL/DDL koşmuyor. `exec_sql` koda gömülü değil.
6. **Git ve disk taraması tükendi:** 30 ayar dosyası, 56 kayıp (dangling) commit, 7 kayıp blob,
   Masaüstü/Belgeler — canlı gizli anahtar hiçbirinde yok. (Tek "service_role" jetonu yerel demoya ait.)

**Sonuç (objektif):** Canlı gizli anahtar/jeton diske hiç yazılı değil; Vercel aracı `.env.production.local`
ve `.env.production` üzerine yazarak yerel kopyaları yok etmiş. Kurtarma için **tek bir kimlik bilgisi**
gerekiyor ve bu Casper'ın kişisel erişimidir — ajan tarafından üretilemez.

**Seçenekler:**
- **A (1 dakika, önerilen):** Casper `supabase.com/dashboard/account/tokens` → "Generate new token" →
  jetonu `C:\Users\Casper\sb-token.txt` dosyasına yapıştırır → ajan Management API ile:
  Ek 1 → doğrula → Ek 2 → doğrula → kategori-şablon eşlemesi → main push → canlı kontrol.
  (Jeton aynı zamanda servis anahtarını okumaya yarar; yerel ayar dosyası da onarılmış olur.)
- **B (jeton yok):** `vercel login` → `vercel env pull .env.production.local --environment=production`.
  Yalnız anahtar "Config" tipindeyse çalışır; "Secret" ise değer gelmez.
- **C (jeton yok, elle):** Supabase paneli → SQL Editor → 2 migration dosyasını yapıştır-çalıştır.

**Anahtardan bağımsız ilerleyebilecek geliştirme:** yeni alanların şema/etiket/arayüz/test tarafı
(menü + pet şablonları, hizmet zenginleştirme) — yalnız "yeni şablon anahtarı" ve "kategori eşlemesi"
veritabanına dokunuyor; o kısım jeton gelince tek adımda tamamlanır.
## 12. Cline doğrulama bandı (2026-09-16 · kod + git kanıtı)

**A) Bu çalışma kopyası `main` DEĞİL.**
- Dizin: `C:\Projects\vixrex` · Dal: `work/product-live-ready-20260914` · HEAD: `6931f004`
- Bu dal, main'in son 8 commitini içermiyor (`git log --oneline HEAD..main`): 5 faz
  (`263bd237`→`2c481f95`), devir belgeleri (`618db640`, `a4e1c8bc`) ve `13046248` (üretime al).
- Sonuç: **canlı ürün kartı işi ve `docs/URUN-KARTI-DEVIR.md` bu kopyada görünmez**; ikisi de `main`de.
- Ayrılma noktası: `6c9d2912` (2026-09-12 14:43) → o günden beri main'e dönülmemiş.

**B) ASIL RİSK — aynı ada sahip İKİ FARKLI dal geçmişi:**
- `git fetch origin` sonrası ölçüm: `origin/work/product-live-ready-20260914` = **`9f66ee51`** (15.09 11:59 +0300).
- Fark: **uzak 40 commit ileri, yerel 5 commit ileri**; ortak ata `adf103ef` (14.09 23:43).
- Uzaktaki 40 commit: `8c0d1678` "align product work branch with current main" (main'in ürün kartını dala aldı)
  + **32 commit toplu yükleme işi** (batch upsert / Excel-CSV / XML import zinciri, 15.09 11:27–11:38) + CI commiti.
  Yazar dağılımı: xpodiumyours 38, Furkan 2.
- Yani **toplu yükleme işi paralel bir hatta ilerleyip push edilmiş**; bu çalışma kopyasında YOK.
- O hattın migration'ları farklı: `20260915109000_snapshot_live_batch_create_products.sql`,
  `20260915110000_product_batch_rich_core.sql` → Codex'in `20260915020000`/`20260915030000` çiftinden BAĞIMSIZ.
  Codex commiti `6ad96d1c` bu uzak dalda yok (kanıt: `git merge-base --is-ancestor 6ad96d1c 9f66ee51` → hayır).

**C) Bugün 4 paralel ürün hattı var:**
1. `main` — canlıda (5 faz, PR #491–495)
2. Bu yerel dal — adf103ef + 5 commit (test onarımı, "canlıya hazır", 3 doküman)
3. Uzak dal `9f66ee51` — main + 32 toplu yükleme commiti
4. Codex worktree `6ad96d1c` — toplu zengin alanlar + görsel yayın tetikleyicisi

**D) Yedek durumu (düzeltmeler):**
- `6ad96d1c` → origin'de VAR ✓ (§1'deki satır düzeltildi)
- Bu yerel dalın 5 commiti → `origin/yedek/durum-raporu-20260916` (= `6931f004`) altında ✓
- `origin/work/product-live-ready-20260914` → yerel kopyadan 40 ileri (fetch ile doğrulandı)

**E) Kaydedilmemiş iş (yalnız Codex worktree'sinde YOK):**
| Worktree | Değişiklik |
|---|---|
| `.codex-worktrees/urun-gorsel-yayin-20260915` | 4 dosya, +31 |
| `.codex-worktrees/vitrin-desktop-width` | `VitrinProfileView.tsx` (10+/10−) |
| `.codex-worktrees/pr341` | `safe_url_launcher.dart` (+16/−23) + yeni `test/safe_url_launcher_test.dart` |

**F) Karar düzeltmesi:** §6'daki görsel kuralı kararı (a), §9'daki **(c')** ile DEĞİŞTİ.

**G) Yol düzeltmesi:** zengin alan editörünün tam yolu `lib/widgets/product/product_rich_fields_editor.dart`
(§7'de kısa yazılmış); dosya hem bu dalda hem `main`de mevcut.

**Kullanılan doğrulama komutları:** `git fetch origin` · `git rev-list --left-right --count origin/work/product-live-ready-20260914...HEAD` ·
`git log --oneline HEAD..main` · `git branch -r --contains` · `git merge-base --is-ancestor` · `git worktree list` ·
`git stash list` · `git -C <worktree> status --porcelain`.
