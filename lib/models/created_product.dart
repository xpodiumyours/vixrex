/// CORE tarafından oluşturulan ürünün değişmez kimlik ve URL bilgisidir.
class CreatedProduct {
  final String id;
  final String slug;

  const CreatedProduct({required this.id, required this.slug});
}
